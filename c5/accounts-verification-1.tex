\textbf{FIELDS:}
account\_number: 0 failures  6 passes  type \cmark  min \cmark  max \cmark  sign \cmark  max\_nulls \cmark
    no\_duplicates \cmark
open\_date: 0 failures  4 passes  type \cmark  min \cmark  max \cmark  max\_nulls \cmark
close\_date: 0 failures  3 passes  type \cmark  min \cmark  max \cmark
title: 0 failures  6 passes  type \cmark  min\_length \cmark  max\_length \cmark  max\_nulls \cmark
    allowed\_values \cmark  rex \cmark
forename: 0 failures  5 passes  type \cmark  min\_length \cmark  max\_length \cmark  max\_nulls \cmark rex \cmark
middle\_name: 0 failures  4 passes  type \cmark  min\_length \cmark  max\_length \cmark  rex \cmark
surname: 0 failures  5 passes  type \cmark  min\_length \cmark  max\_length \cmark  max\_nulls \cmark rex \cmark
address1: 0 failures  6 passes  type \cmark  min\_length \cmark  max\_length \cmark  max\_nulls \cmark
    no\_duplicates \cmark  rex \cmark
city: 0 failures  5 passes  type \cmark  min\_length \cmark  max\_length \cmark  max\_nulls \cmark  rex \cmark
postcode: 0 failures  6 passes  type \cmark  min\_length \cmark  max\_length \cmark  max\_nulls \cmark
    no\_duplicates \cmark  rex \cmark
home\_tel: 0 failures  6 passes  type \cmark  min\_length \cmark  max\_length \cmark  max\_nulls \cmark
    no\_duplicates \cmark  rex \cmark
mobile\_tel: 0 failures  6 passe  type \cmark  min\_length \cmark  max\_length \cmark
    max\_nulls \cmark  no\_duplicates \cmark  rex \cmark
email: 0 failures  6 passes  type \cmark  min\_length \cmark  max\_length \cmark  max\_nulls \cmark
    no\_duplicates \cmark  rex \cmark
account\_type: 0 failures  6 passes  type \cmark  min\_length \cmark  max\_length \cmark
    max\_nulls \cmark  allowed\_values \cmark  rex \cmark
overdraft\_limit: 0 failures  5 passes  type \cmark  min \cmark  max \cmark  sign \cmark  max\_nulls \cmark
cash\_card\_number: 0 failures  6 passes  type \cmark  min\_length \cmark  max\_length \cmark
    max\_nulls \cmark   no\_duplicates \cmark  rex \cmark

\textbf{DATASET:}
Extra (disallowed) fields: None    Missing (required) fields: None

\textbf{SUMMARY:}
Constrained Fields: 16             Failing Fields: 0 (0.00%)
Constraints: 87                    Failing Constraints: 0 (0.00%)
Extra (disallowed) fields: 0       Missing (required) fields: 0
